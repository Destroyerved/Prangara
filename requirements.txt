# Superseded by backend/requirements.txt, which is what the running
# service installs. Kept only so an existing checkout does not break.
# PRANGARA — legacy root requirements
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
bcrypt>=4.1

# Report build (docs -> PDF). Rendering uses headless Chrome/Edge, which the
# build script locates automatically; no pandoc or LaTeX needed.
Markdown>=3.5

# Tests
pytest>=8.0
httpx>=0.27
