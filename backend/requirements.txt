# PRANGARA backend - platform API and deterministic engine

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
email-validator>=2.0
python-multipart>=0.0.6

SQLAlchemy>=2.0.30
alembic>=1.13
# PostgreSQL driver. Not needed for the default SQLite development database.
# psycopg[binary]>=3.1

bcrypt>=4.1
PyJWT>=2.8

# Object storage. Only needed when STORAGE_BACKEND=minio.
# minio>=7.2

# Used to call a local Ollama runtime for conversational intake, when one is
# configured. Intake degrades to a deterministic parser without it.
httpx>=0.27

# Tests
pytest>=8.0

# Database: Google Cloud Firestore
google-cloud-firestore>=2.16.0
